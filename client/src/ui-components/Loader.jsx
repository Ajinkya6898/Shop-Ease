const Loader = () => {
  return (
    <div className="fixed inset-0 flex items-center justify-center bg-white/50 backdrop-blur-[1%] z-50">
      <div className="loader"></div>
    </div>
  );
};

export default Loader;
