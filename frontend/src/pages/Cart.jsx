import React from "react";
import Alert from "../ui-components/Alert";

const Cart = () => {
  return (
    <div>
      <Alert type="info" message="Nothing in cart" />
    </div>
  );
};

export default Cart;
